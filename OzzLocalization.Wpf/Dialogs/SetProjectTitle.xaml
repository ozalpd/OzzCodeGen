<Window x:Name="window" x:Class="OzzLocalization.Wpf.Dialogs.SetProjectTitle"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OzzLocalization.Wpf.Dialogs"
        mc:Ignorable="d"
        Title="Localization Project" Height="120" Width="360"
        WindowStyle="ToolWindow" Activated="Window_Activated">
    <Grid DataContext="{Binding ElementName=window, Mode=OneWay}">
        <Grid.RowDefinitions>
            <RowDefinition Height="8"/>
            <RowDefinition Height="36"/>
            <RowDefinition Height="28"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding Project.FullPath}"
                   Grid.Row="1" Grid.ColumnSpan="2" Margin="4" TextWrapping="Wrap"/>
        <TextBlock Text="Project Title:" Grid.Row="2" Grid.Column="0" Margin="4"/>
        <TextBox Text="{Binding Project.Title}" Name="txtTitle"
                 Grid.Row="2" Grid.Column="1" Margin="2"
                 HorizontalAlignment="Stretch"
                 TabIndex="0"/>


    </Grid>
</Window>
